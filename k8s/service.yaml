apiVersion: v1
kind: Service
metadata:
  name: newrelic-test-service
  labels:
    app: newrelic-test-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: newrelic-test-app